VITE_API_URL=http://localhost:5000/api
VITE_KIOSK_API_KEY=replace_this_too
# WebSocket exposed locally by device-bridge (runs on the same Pi as this app)
VITE_DEVICE_BRIDGE_WS=ws://localhost:6060
# Set to "true" to develop the UI without any hardware attached
VITE_MOCK_HARDWARE=true
